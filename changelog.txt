0.4.0
* high level parsing for command arguments
* int, uint, hex, hexbuf, string argument types
* validating arguments values range
* configurable optional/required arguments parsing
* some bugs fixing in command parsing algorithm
* working buffer always ends with '/0' char
* allow special characters in commands names
* more units tests added
* example application added

0.3.0
* independent buffers (parsing buffer and state buffer)

0.2.0
* passing command struct instead of command name in command handlers